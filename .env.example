# DurableFlow environment variables.
# Copy this file to `.env` and populate real values. `.env` is gitignored.

# LangSmith telemetry adapter (optional). Enables export when
# DURABLEFLOW_LANGSMITH_ENABLED is set to 1/true/yes. Leave unset to keep
# DurableFlow fully local with no network calls.
LANGSMITH_API_KEY=

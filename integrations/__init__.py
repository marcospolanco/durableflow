"""Optional integration adapters for DurableFlow.

This package is import-safe with no optional dependencies installed. LangSmith
integration is opt-in: see ``integrations.langsmith_adapter``. The Aegis Gateway
client is opt-in: see ``integrations.aegis_client``. Core must not import either.

"""
